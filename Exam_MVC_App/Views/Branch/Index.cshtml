@model List<Exam_MVC_App.Dtos.BranchDtos.BranchDto>


@if (TempData["Message"] != null)
{
    <div class="alert alert-success">
        @TempData["Message"]
    </div>
}


<div class="container">
    <h1>
        All Branches
    </h1>

    <table class="table table-light table-hover">
        <thead>
            <th>#</th>
            <th>Name</th>
            <th>Manager</th>
            <th>Actions</th>
        </thead>    

        <tbody>
            @foreach (var branch in Model)
            {
                <tr>
                    <td>@branch.BranchId</td>
                    <td>@branch.BranchName</td>
                    <td>@branch.Manager.Fname @branch.Manager.Lname</td>
                    <td>
                        <a href="/Branch/Edit/@branch.BranchId" class="btn btn-secondary">Edit</a>
                        <a href="/Branch/Delete/@branch.BranchId"
                           class="btn btn-danger"
                           onclick="return confirm('Are you sure you want to delete this branch?');">
                            Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


